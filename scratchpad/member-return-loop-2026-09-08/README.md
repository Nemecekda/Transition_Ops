# Member-return loop: local candidate

2026-09-08. S3-devops. Branch `codex/member-return-loop`, isolated repository `/tmp/tops-member-return-2026-09-08`, based on fresh authenticated remote main `e9a84fe2c94cecd4b76b70880fc3a76a36e1e469` (tree `2c6fb29204e548d2351591cb91ad7ed25d316716`, identical to released 0fd).

**LOCAL AUTOMATION PASS.** The fixed eight synthetic return episodes improved from 6/8 to 8/8 successful continuations. Failures fell from 2/8 to 0/8: 100% failure reduction, +25 percentage points in success. The >=50% target is met. This measures these functional cases only; member retention and overall product improvement are unmeasured.

[Review the exact local dist build](http://127.0.0.1:48158/). Server session 60048; localhost only. The earlier parent source preview remains at port 48157. No hosted candidate, push, merge, deployment, live generation, notification, or production mutation was performed.

## Changes

- Current canonical milestones and documents own text, ordering and membership. Lazy state initialization overlays only strict saved booleans at matching IDs before the first persistence effect. Unknown IDs and non-boolean flags cannot override content; malformed keys are isolated. Duplicate IDs use the first valid boolean, including false. Existing array storage shapes and keys remain unchanged.
- Home displays task/document counts and native continuation buttons. The first partially completed checklist in canonical order takes priority. Untouched and fully completed states offer ordinary review routes without inventing urgency, eligibility or deadlines. The separate Guard dashboard is preserved.
- Home Timeline, Readiness and Next Action use native buttons. Next Action expands the exact existing reminder, clears blocking transient filters and focuses its header. Continuation destinations receive focus; returning Home restores the corresponding invoker.
- Cache 156 -> 157 only; app release-note version v96 -> v97 with one note describing the actual change. The privacy regression now requires the current version to match the newest note and rejects a mismatched-version negative control. Its historical release-note and current push-off assertions remain intact.

## Frozen comparison

The supplied coverage file was concurrently replaced by a conflicting 12-case/SITREP revision. The parent task's earlier command readback preserved the original U1-U8 protocol. [Recovered v1 excerpt](protocol-v1-recovered.md) follows the latest direct instruction: eight cases, SITREP redesign excluded. Its prose quote glyphs are normalized in this repository copy; the original readback remains in the coordinator directory.

Exact [fixtures](fixtures-v1.json) were bound before baseline or app edits. SHA-256 `59e94221f93c56ba8186de3cf162e018bbf65712f1a912f57433642a8944b9dc`. Clock `2026-09-08T15:00:00.000Z`, America/Chicago, Chrome 152.0.7977.82, 390x844. Each case has one binary result, with at most two navigation activations through any visible route. Scrolling and focus are logged separately; a new card is not itself a pass condition.

| Episode | Baseline | Candidate | Navigation activations B/C |
|---|---|---|---|
| U1 remaining current task | PASS | PASS | 2 / 1 |
| U2 second task after first completed | PASS | PASS | 2 / 1 |
| U3 obtain another document | PASS | PASS | 2 / 1 |
| U4 exact applicable reminder | PASS | PASS | 2 / 1 |
| U5 legacy task text | FAIL: obsolete text | PASS | 2 / 1 |
| U6 legacy document text | FAIL: obsolete text | PASS | 2 / 1 |
| U7 previously edited separation date | PASS | PASS | 2 / 1 |
| U8 post-separation task | PASS | PASS | 2 / 1 |

Baseline allowed the existing Home More Tools -> Documents route. No baseline PASS regressed. [Baseline](baseline.json), [final candidate](candidate.json), [independent keyboard replay: 8/8](candidate-keyboard.json). Final pointer run includes worker157 identity; the provisional pointer result preceded the cache bump and is retained separately.

An initial local server error prevented measurement. A subsequent runner incorrectly clicked the wrapper around MARK COMPLETE; its invalid U4 failure is retained in `baseline-run1-selector-error.json`. The selector was corrected and all eight baseline cases rerun on the unchanged, hash-asserted e9 source before app edits. Fixtures, expected outcomes, denominator, modality and budget did not change.

## Validation and preservation

[Validation summary](validation-summary.json) links passing logs for all five required commands: OpenAI migration, service-worker privacy, privacy/network, runtime spend and accessibility release. `npm run build:public` passes with exactly 22 runtime files; [manifest](public-manifest.json) hashes every file and records root netlify.toml separately, outside dist. This is local inventory evidence, not a hosted 22+1 manifest claim.

[27 adverse controls](adverse.json) pass: wrong shapes/JSON, strict booleans, duplicates, removed and omitted/new canonical IDs, read failure, corrupt-key isolation, no transient mark clobber in browser mount writes, completion/reopen/reload, empty/all-complete states, exact reminder with blocking filters, keyboard focus, 390/320/1280 geometry, storage denial, site-data clearing, and audience preservation. Earlier loader fixtures keep their expected JSON values; the candidate tests exercise its extracted real hydration helpers. Repeated test setup writes belong only to disposable harness profiles.

The baseline/candidate raw-source runner observed existing Google Fonts attempts and blocked them. No application function/provider, GA, Kit or OneSignal call was made by these episodes. The existing privacy/network suite separately verifies current and migrated cohorts with zero provider requests. No browser account profile or member data was used.

[Structural checks](structural.json) parse the inline blocks, tracked JS/JSON and intended test additions. Changed-line encoding and diff whitespace checks pass. YAML/4S and function packaging 4N are not triggered by this change. A late CDP target-loss interrupted the first accessibility run; the complete clean rerun passed without a runtime change. The first privacy failure was the obsolete v96 assertion described above; a sandbox localhost denial was retried with authorized test-server access.

[Preservation and observations](preservation-and-blindspots.json): canonical policy/checklist/reminder literals, snapshot logic, protected Resume state and historical release notes are unchanged; the worker differs only by its cache integer. All other preexisting tracked runtime, backend, Privacy, push, configuration and skill files are unchanged. The new test-only release-version assertion is the only existing script change. No new dependency was installed; node_modules points to the already validated local package set. Node v24.18.1/npm 11.16.0.

Known SITREP blind spots remain: six-hour opens can advance an unreviewed snapshot, a count increase can disappear on reload, same-count replacements are invisible, and every existing summary item still opens the policy panel. Morning/noon and both DST transition probes of the unchanged calendar helper pass. None of these observations claims a SITREP fix or re-verifies policy. Stable IDs whose meaning changes still need a content/ID decision; an old completion mark cannot prove the new task was performed.

[Shared preservation](shared-preservation.json) confirms the shared root remains tracked-clean clone 0fd, with its exact index/refs, four stashes and 19 untracked files unchanged. No other agent's files were edited.

## Cache and next action

[Fresh cache ledger](cache-ledger.json) joins the prior completed history with the available deployment delta. Production remains e9/deploy `6aa039a255936800098c8e74`, clone remains 0fd/deploy `6aa031d0c00efd000812f107`; both highs are 156. Public production worker bytes match. No served 157 owner or conflicting new worker was found. Deleted/unreturned deployment history remains unobservable.

157 is allocated to this ordinary feature candidate. The old network-only 157 emergency recovery artifact was never applied or reserved. It must not later be applied after this differing worker ships; recovery for a future frozen release needs a fresh binding/integer. No recovery was applied or prepared in this feature turn. Recheck the two-origin ledger before any hosted handoff.

**Next: parent independently reviews this local UI and exact feature diff.** The local commit is prepared under existing authority; do not push or merge it in this task. Parent review, a future immutable preview, and applicable manual/hosted acceptance are separate. Earlier narrow Dean VoiceOver results remain attached to their earlier builds; this packet does not upgrade them to a new manual AT or full release PASS.

Repeat the fixed candidate jobs with `node scratchpad/member-return-loop-2026-09-08/browser-episodes.cjs candidate` (append `--keyboard` for the independent keyboard matrix). Set `TOPS_TEST_ROOT` to an unchanged e9 checkout for a baseline replay; the runner rejects a changed baseline. The freeze script refuses to overwrite the fixture file. Test servers need localhost permission and always block outbound traffic.

COMPLETED: local feature, fixed comparison, adverse/required gates and review build. PENDING: independent parent UI/diff review and any later authorized hosted work. BLOCKED: no local implementation blocker; manual/hosted release results unverified. REGISTRY CHANGES: none. BURN: session token cost not reliably measurable; no live AI/provider calls.
